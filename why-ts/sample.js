// const myAdd = (a, b) => {
//   return a + b;
// };

// // add(10, "20");
// let myResult = myAdd(10, 20);
// myResult.toLocaleString();

// @ts-check

/**
 *
 * @param {number} a 첫번째 숫자
 * @param {number} b 두번째 숫자
 * @returns
 */

const sum1 = (a, b) => {
  return a + b;
};

// sum1(1, "20");
